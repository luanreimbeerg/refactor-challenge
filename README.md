# Refactor the code for typeScript

- Install the typescript as a dependency

```ts 
    yarn add typescript -D
```

- Install tsc --init

```ts 
    yarn tsc --init
```

## Take configuration of the ts.config